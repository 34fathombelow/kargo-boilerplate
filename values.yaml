# Default values for kargo-boilerplate.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

project:
projectConfig:
  promotionPolicies: []
warehouse:
  subscriptions: []
stages: []
analysisTemplates: []